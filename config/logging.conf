[general]
# 日志格式
pattern = [%Y-%m-%d %H:%M:%S.%e] [%l] [%t] %v
# 是否显示在控制台
console = true
# 是否写入文件
file = true

[file]
# 日志文件路径（相对于程序根目录）
path = logs/stability.log
# 单个日志文件最大尺寸（MB）
max_size = 10
# 最大保留文件数
max_files = 10
# 是否使用日期作为文件名后缀
use_date = true
# 日期格式（用于文件名）
date_format = %Y%m%d 